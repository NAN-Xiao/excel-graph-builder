# Indexer - 配置表图谱构建服务依赖
# 安装命令: pip install -r requirements.txt

# ========== 核心依赖（必需）==========

# Excel 文件解析
pandas>=1.3.0
openpyxl>=3.0.0

# 定时任务调度
schedule>=1.1.0

# 文件系统监控
watchdog>=2.1.0


# ========== 可选依赖（推荐安装）==========

# 模糊匹配：用于发现 usr_id ↔ user 这类简写变体的关联
# 安装后自动启用，不安装则使用严格模式匹配
python-Levenshtein>=0.12.0


# ========== 性能优化说明 ==========

# 1. 大数据量场景（2000+ 表，大文件）：
#    - 已内置：修改时间优先增量、列级采样哈希、内存优化
#    - 无需额外依赖

# 2. 图算法分析（环检测、模块聚类）：
#    - 已内置：使用标准库实现，无需 networkx
#    - 如需更复杂算法可安装：networkx>=2.6.0

# 3. 机器学习增强（可选，当前未使用）：
#    - scikit-learn：列内容向量化相似度
#    - 当前使用启发式算法，无需安装
